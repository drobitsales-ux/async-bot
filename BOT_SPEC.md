# Торговый бот: описание работы, стратегии, фильтры
> Код: **async_bot v59 / bybit_worker v59** | Документ от 2026-08-05 (обновлён по итогам v53–v59)
> Раздел §2 — результаты аудита 2026-07-31 с пометками об исправлении. Раздел §2.9 — новые находки v58–v59.

---

## 1. Архитектура

| Компонент | Роль | Биржа |
|---|---|---|
| `async_bot.py` | Генератор сигналов, все стратегии, БД сделок, отчёты | BingX |
| `bybit_worker.py` | Исполнитель сигналов, **только SA** | Bybit (боевой проп-счёт) |

**Поток:** `async_bot` сканирует рынок → фильтры → AI-оракул (advisory) → открытие на BingX → HTTP-публикация воркеру → воркер применяет свой risk-сайзинг и открывает на Bybit.

**Правило синхронизации:** логика управления позицией из `async_bot.py` обязана зеркалиться в `bybit_worker.py`. Исторически главный источник багов (последний случай — §2.9.2, notional-cap с ROUND вместо TRUNCATE).

**Изоляция воркера (v40):** `ALLOWED_STRATEGIES = ('SA',)`.

---

## 2. 🔴 РЕЗУЛЬТАТЫ АУДИТА 2026-07-31 (историческая запись, с пометками фикса)

### 2.1. Фатальный баг: стратегия RB не может дать сигнал НИКОГДА — ✅ исправлено v53

В `rb_signal()` окно диапазона считалось срезом, который **включал саму свип-свечу**:

```python
range_low = np.min(l[-(RB_RANGE_BARS + 1):-1])   # = l[-49:-1] → включает бар [-2]
lo2 = l[-2]
if lo2 < range_low:                              # ← НИКОГДА не истинно
```

**Исправлено v53:** срез `[-(RB_RANGE_BARS + 2):-2]`, исключает оба бара. **v55:** RB вынесен из `scan_smc()` в отдельный круглосуточный сканер `scan_rb()` (раньше был слеп 56% суток вне сессии SMC). **v56:** гейт `RB_MAX_RANGE_ATR` 5.0→8.0 (порог требовал рынка тише случайного блуждания — симуляция: медиана range/ATR≈11.7 на 48 барах, доля ≤5.0 ~0.1%) + логирование медианы/p25 `range_w_atr` по всем просканированным символам для последующей эмпирической калибровки (данные копятся, решения по порогу пока нет).

### 2.2. Ошибка калибровки: SA_MIN_RR 0.7→0.5 (v46) навредил — ✅ исправлено v53

Сегмент «RR входа» дал прямой вердикт: **`lt0.7: 7 сд | WR 0% | Avg −2.54% | PF 0.00`**.

**Исправлено v53:** порог возвращён на 0.7 (текущий дефолт).

### 2.3. ⚠️ «Золотая зона» 1.8–2.2 ATR распалась на форварде — статус: под наблюдением, НЕ трогать

| | было (n=20) | стало (n=28) | новые 8 сделок |
|---|---|---|---|
| WR | 80% | 61% | 1 победа из 8 |
| PF | 14.41 | 1.00 | — |

Тот же сценарий, что с PB: in-sample сегмент схлопнулся к PF≈1.0 на форварде. Потолок `SA_ATR_DIST_MAX=2.2` (v42) построен на этом же in-sample наблюдении и **остаётся неизменным** — расширять его без данных запрещено (см. §2.9.1: попытка проверить эту гипотезу через SA_SHADOW в v58 её НЕ подтвердила, см. ниже).

### 2.4. Общая деградация всех живых стратегий (снимок на 07-31, устарел)

Оставлено для истории — обе стратегии на момент аудита сходились к PF≈1.0. Актуальную картину смотреть в `/stats_analyze` (числа с тех пор изменились: риск SA снижен вдвое в v54, фильтры объёма починены в v55/v57).

### 2.5. Нижняя граница объёма SA (1.3) не была валидирована — ✅ данные собраны v53, вывод v58: НЕ снижать

Shadow-логирование отсеянных сетапов (`SA_SHADOW`, добавлено v53, расширено v57 на `vwap_far`/`no_setup`) дало первый результат на n=15 (BOT_SPEC §2.9.1): зона `vol<1.0` PF 1.08, Avg≈0 — **никакой скрытой прибыли не содержит**. Порог 1.3 остаётся. n=15 < 30 — продолжаем копить shadow-данные, не полагаться как на окончательный вывод, но пока нет оснований трогать порог.

### 2.6. RSI: ноль сделок за всю историю — частично адресовано v57, требует данных для подтверждения

Цепочка причин на момент аудита: объём отсекал 73–77 из 80 через `vol_ratio = mean(v[-4:-1]) / mean(v[-23:-5])` — **`mean`-база на 18-барном окне заражалась любым историческим спайком и держала знаменатель задранным 18-20 баров**, независимо от текущей рыночной активности.

**Исправлено v57:** база (`v[-23:-5]`) переведена с `np.mean` на `np.median` для SMC/RSI/MOM/PB/RB (SA получила тот же фикс раньше, в v55). Синтетический тест (18-бар база + спайк 4-12x в случайном баре, отбор только сценариев, валидных без спайка): mean-база блокирует ~99% таких сигналов, median-база — ~10%. Направление и масштаб эффекта совпадают с логами (`[RSI SCAN]` резал 94% символов). Реальное число сделок RSI после фикса пока не накоплено — следить в `/stats_analyze`.

Конструктивное противоречие (объёмный фильтр требует сигнатуру тренда, RSI MR ловит истощение ПОСЛЕ климакса) актуальности не потеряло — median решает проблему заражения одним спайком, но не отменяет саму механику; если после сбора данных сделок всё ещё околонулевое число — вопрос к самой механике фильтра, не к базе усреднения.

### 2.7. Аномалия длительности SL-сделок — ✅ исправлено v56

`SL: 20 сд | Avg −1.93% | 379 мин` при `MAX_TRADE_MIN_SA = 150`.

**Корень (диагностирован v56):** `tp50_hit=True` устанавливался в памяти БЕЗ подтверждённого размещения BE-стопа на бирже — в двух путях (неделимый лот и основной путь TP50) сбой `create_order`/`trading_stop` не откатывал флаг. `tp50_hit=True` снимает и обычный, и smart-таймаут → позиция оставалась без реальной защиты и без таймаута одновременно.

**Исправлено v56** в обоих ботах: `tp50_hit`/`be_moved` теперь ставятся ТОЛЬКО после подтверждённого ответа биржи; при сбое — `logging.error` + Telegram-алерт, позиция остаётся под защитой таймаута. Добавлен диагностический флаг `pos['sl_on_exchange']`, логируется при закрытии сделки.

### 2.8. Мёртвый код — не исправлено, малый приоритет

`_min_quote = 5000.0 if not False else 20000.0` (async_bot.py, `rsi_signal()`) — условие константное, всегда даёт 5000. Не влияет на поведение (веткa `20000.0` недостижима), исправление отложено как не влияющее на торговлю.

### 2.9. Находки v58–v59 (новые с момента аудита)

#### 2.9.1. SA_SHADOW: гипотеза об ATR-нормировке НЕ подтвердилась

Расширение SA_SHADOW на `vwap_far`/`no_setup` (v57) дало первые n=15 по зонам, которые реальные фильтры SA никогда не пропускали. Результат (v58): **все отсеянные зоны показывают отсутствие edge**, не потерянную прибыль:

| Зона | PF | Avg |
|---|---|---|
| `vol<1.0` | 1.08 | ≈0 |
| `RR<0.5` | 0.82 | ≈0 |
| `dist 1.5–2.2 ATR` | 1.05 | ≈0 |
| `ATR<0.2%` (низкий режим волатильности) | 0.76 | ≈0 |

Гипотеза «упавший ATR раздувает dist_atr и режет прибыльные входы потолком 2.2» — не подтвердилась. **Действие: SA_ATR_DIST_MAX, vol 1.3–2.0, SA_MIN_RR=0.7, RSI 30/70 остаются без изменений.** n=15 < 30 — продолжать копить данные, но текущее направление (не трогать) данными подтверждено, а не просто отложено.

#### 2.9.2. Немые `return` в execute() — ✅ исправлено v58

Сигнал SMC DASH/USDT прошёл AI-оракула (advisory, conf=100≥30), но сделка не открылась без единой строки в логах — причина: несколько `return` в `execute()` не логировали причину отказа. Аудит функции сверху донизу нашёл 4 таких случая (лимит суммарных позиций, лимит позиций по направлению, `free_usdt<50`, `sl_dist<=0`) — всем добавлен `logging.warning` с причиной и числами, формат `'[{strategy}] {sym}: <причина> — пропуск'`.

#### 2.9.3. AI-оракул: защита от шаблонных галлюцинаций — ✅ добавлено v58

Groq документированно возвращал дословно один и тот же `comment` (`"REJECT (100/100) | RSI > 68"`) на разных символах (ADA @ RSI 66.1, XLM, DASH) независимо от факт. чисел сетапа. Для SMC безвредно (advisory при conf≥30), но для SA/RSI (порог блокировки 55) такой вердикт блокировал реальный вход по выдуманному основанию.

**Добавлено v58:**
- Детектор повторов: если последние 3 вердикта оракула несут дословно одинаковый `comment` на ≥2 разных символах — вердикт помечается `advisory` (не блокирующим) для **всех** стратегий, не только SMC.
- `AI_BLOCK` (ENV, дефолт `1`) — при `AI_BLOCK=0` оракул полностью advisory (только логируется). Ручной аварийный рубильник на случай нового паттерна галлюцинации, который детектор повторов не ловит.

#### 2.9.4. Notional-лимит систематически отклонял валидные сделки на копейки — ✅ исправлено v59

Сигнал SMC DASH/USDT: `notional $75.5 > max $75.4 (20% маржи × 5x плечо от $75) — пропуск`. Превышение $0.10 = 0.13%.

**Механизм (см. §2.9.5 — новое правило методологии):** `max_notional = free × LEVERAGE × MARGIN_PCT_ALT`. При `MARGIN_PCT_ALT=0.20` и `LEVERAGE=5` → `cap = free × 1.00` (весь баланс). При риске 1% на сделку и SL ровно на полу `MIN_SL_PCT=1.0%` → расчётный `notional` **ровно равен** `cap`. Округление к ближайшему шагу лота (`ccxt.ROUND`, v39, само по себе корректное решение) вверх выталкивает за лимит на копейки — и это происходит систематически при каждой сделке с SL на полу в тихом рынке, не изредка.

**Исправлено v59:** `execute()` (async_bot.py) теперь урезает `qty` под `max_notional` вместо отказа — округление TRUNCATE (строго вниз, гарантия влезть в лимит), риск при урезании только падает (безопасно для проп-счёта, отдельный проп-guard 1.30× ловит обратный случай — завышение риска). `bybit_worker.py` уже урезал позицию в аналогичной ситуации, но капающий шаг использовал `ROUND` — тот же класс бага одним уровнем ниже; переведён на TRUNCATE.

---

## 2.9.5. НОВОЕ ПРАВИЛО МЕТОДОЛОГИИ (v59)

**Перед внедрением любого нового лимита — прогонять его на связке с уже действующими лимитами, проверять пересечения.**

Два по отдельности разумных лимита (`MIN_SL_PCT` — пол SL от v37, `MARGIN_PCT_ALT × LEVERAGE` — потолок notional от v45) были введены в разных деплоях без проверки совместной границы. На пересечении (SL на полу + консервативный риск) они сходятся в одну точку `notional == cap`, и любое округление лота выталкивает результат за пределы допустимого диапазона. Диапазон допустимых исходов схлопнулся в точку — по построению неустойчиво к округлению, шуму цены, любому дрожанию входных чисел.

**Практическое правило:** при добавлении нового ENV-порога или guard — явно посчитать (аналитически или на диапазоне сценариев), не образует ли он с уже действующими лимитами вырожденный, пустой или граничный (совпадающий на границе, а не строго вложенный) диапазон допустимых значений. Если образует — либо развести лимиты (не давать сойтись в точку), либо на границе смягчать (урезание), а не жёстко отказывать. См. §2.9.4 как образец: `max_notional` пересекался с `MIN_SL_PCT × RISK_PER_TRADE` в точности, фикс — смягчение (cap вместо reject), не только правка одного числа.

Добавлено в §8 «Методология» пунктом 8.

---

## 3. Стратегия SA — Single-Asset VWAP Mean Reversion

**Инструмент:** BTC/USDT:USDT. **ТФ:** 15m. **Статус:** live, риск снижен вдвое (v54) на время сбора данных для проверки edge; заблокированные зоны провалидированы shadow (§2.9.1) — текущие фильтры данными подтверждены, не просто оставлены по умолчанию.

### Фильтры входа (порядок в коде важен — срабатывает первый)

| № | Фильтр | Значение | Reason | Статус обоснования |
|---|---|---|---|---|
| 1 | Объём | `1.3 ≤ vol_ratio ≤ 2.0` (база — median, v55) | `vol_climax` | Верхняя граница доказана (Fisher p=0.0002, n=63). Нижняя (1.3): SA_SHADOW n=15 PF 1.08 Avg≈0 — **порог подтверждён, не снижать** (§2.9.1) |
| 2 | Потолок дистанции | `\|dist_atr\| ≤ 2.2` | `vwap_far` | In-sample p=0.007, форвардом не подтверждён (§2.3). SA_SHADOW n=15 в зоне 1.5–2.2 не найденной прибыли не подтвердил (§2.9.1) — **не расширять** |
| 3 | Сетап | `\|dist_atr\| ≥ 1.5` и RSI ≤ 30 / ≥ 70 | `no_setup` | базовое определение |
| 4 | Минимальный RR | `RR ≥ 0.7` | `low_rr` | Подтверждён форвардом (RR<0.7 → 0/7) И shadow (§2.9.1: RR<0.5 PF 0.82) |

### Расчёт уровней

```
sl_pct = clip(ATR/price × 2.0, MIN_SL_PCT=1.0%, MAX_SL_PCT=2.5%)
TP     = VWAP (плавающая цель)
RR     = |TP − price| / |price − SL|
```

**Структурное следствие (см. также §2.9.4/§2.9.5):** SL зажат снизу в 1.0%. На полу SL при риске 1% (SMC/RSI) notional-расчёт сходится ровно к марж-лимиту — источник систематического отказа сделок, исправлено в v59 урезанием вместо отказа.

### Логика выхода

| Механизм | Условие |
|---|---|
| TP50 | ≥50% пути к TP → фиксация 50% + SL→БУ, **подтверждённый на бирже** (v56) |
| TP50 при неделимом лоте (v43, ужесточён v56) | `qty=0.001` → весь объём, SL→БУ, `tp50_hit` ставится только при подтверждённом ордере |
| Фронтран БУ | 80–100% пути к VWAP → SL→БУ |
| Smart Timeout | >75 мин, не tp50_hit, pnl≤0, MFE≤0.40% |
| Обычный таймаут | >150 мин и не tp50_hit — теперь безопасно: tp50_hit гарантированно означает реальный БУ на бирже (v56) |
| Трейлинг | после TP100, 0.8×ATR |

---

## 4. SMC — Smart Money Concepts (BingX)

Снимок на аудит 07-31: 28 сделок, WR 21%, PF 1.01. Объёмная база переведена на median в v57 (тот же фикс, что и для RSI/SA) — числа выше устарели, актуальные смотреть в `/stats_analyze`.

Цепочка: `news → vol (median-база с v57) → struct → choch → short_blk → vwap/rsi/adx → rsi_exh → alt_hi → fvg`.

**Открытый вопрос (с v44):** сделка ADA вошла при RSI 66.1 при заявленной зоне 55–65 — диагностирован как AI-галлюцинация оракула (§2.9.3), не баг фильтра: сам фильтр SMC работает по факт. RSI, оракул для SMC advisory и не блокирует. Детектор шаблонных ответов (v58) снижает вероятность повтора паттерна незамеченным.

---

## 5. RSI (BingX)

0 сделок на момент аудита (§2.6). Причина частично устранена в v57 (median-база объёма) — накопление данных продолжается, окончательный вывод не сделан.

---

## 6. Shadow / отключённые

| Стратегия | Статус | Причина |
|---|---|---|
| **RB** | shadow, **работает** (с v53) | Фикс среза (§2.1) + вынесен в круглосуточный сканер (v55) + порог `RB_MAX_RANGE_ATR` пересмотрен на 8.0, данные копятся на калибровку (v56) |
| **PB** | shadow, в live не выводить | 200 сд: Long PF 0.52, Short PF 0.41. Все «кандидаты» схлопнулись при росте n |
| **MOM** | отключена | 222 shadow-сделки, edge нет. Объёмная база также переведена на median (v57), но стратегия остаётся выключенной |
| **1h-бот** | отключён | 154 сделки, edge нет |
| **SA_SHADOW** | shadow, активна с v53, расширена v57 | Логирует SA-сетапы, отсеянные `vol_climax`/`low_rr`/`vwap_far`/`no_setup`, без риска деньгами. n=15, см. §2.9.1 |

---

## 7. Риск-менеджмент

| Механизм | Значение |
|---|---|
| Риск на сделку (BingX, SMC/RSI/PB/MOM) | 1% депозита (`RISK_PER_TRADE`, `qty = risk/sl_dist`) |
| Риск на сделку SA (оба бота) | **0.5%** (`SA_RISK_MULT=0.5` / воркер `RISK_PCT=0.005`) — снижено вдвое в v54 на время проверки edge |
| Плечо | 5x |
| Максимум позиций | 3 суммарно (`MAX_TOTAL_POS`), 2 на направление (`MAX_PER_DIR`) |
| Дневной лимит DD | 2.5% |
| Лимит маржи | `MARGIN_PCT_SA` (дефолт 0.30), `MARGIN_PCT_ALT` (дефолт 0.15) — ENV |
| Округление лота (основной расчёт) | `ccxt.ROUND` (дефолт ccxt — TRUNCATE, терял до 44%) — корректно, не менять (v39) |
| Округление при урезании под notional-лимит | `ccxt.TRUNCATE` — обязательно вниз, иначе округление возвращает за лимит (v59, §2.9.4) |
| Проп-guard (риск) | пропуск при факт. риске > 1.30× целевого |
| Notional-лимит | **урезание qty под лимит вместо отказа** (v59) — риск при урезании только падает |
| AI-оракул блокировка | `AI_BLOCK=1` (дефолт) — блокирует при reject; детектор шаблонных ответов принудительно снижает до advisory; `AI_BLOCK=0` — оракул всегда advisory (v58) |

### Ограничения вне кода

**Неделимый лот BTC.** При депозите ~$78 и риске 0.5% (после v54) позиция Bybit близка к минимуму лота 0.001. TP50 частично невозможен, обход — весь объём под БУ, подтверждённым на бирже (v43, ужесточён v56).

**AI-оракул ненадёжен, но теперь с защитой.** Задокументированы шаблонные галлюцинации (§2.9.3). С v58 детектор повторов + `AI_BLOCK` рубильник снижают риск ложной блокировки; сам факт ненадёжности вывода оракула не устранён — используется строго как advisory-сигнал, не как источник истины.

---

## 8. Методология (нарушения обошлись дорого)

1. **n ≥ 30 до внедрения**, n ≥ 15 для кандидата к наблюдению.
2. **Fisher exact + Wilson CI.** Пересекающиеся CI = фильтра нет.
3. **In-sample ≠ forward.** Сегмент, найденный на выборке, обязан подтвердиться на новых данных до внедрения фильтра. Нарушено дважды: PB (ADX 60+), SA (зона 1.8–2.2) — оба раза edge исчез.
4. **Проверять популяцию калибровки.** Порог, откалиброванный на одной популяции, нельзя применять к другой (ошибка SA_MIN_RR, §2.2).
5. **Механическое обоснование обязательно.**
6. **Одно изменение — один деплой.**
7. **Отсутствие данных ≠ отсутствие edge.** Собирать shadow, а не гадать (пример решённый данными — §2.9.1).
8. **Лимиты пересекаются — проверять на связке, не по одному.** Перед внедрением нового ENV-порога/guard считать, не образует ли он с уже действующими лимитами вырожденный (сходящийся в точку) или граничный диапазон допустимых значений. Если образует — разводить лимиты или смягчать границу (урезание вместо отказа), а не только чинить конкретное число. См. §2.9.4/§2.9.5 (`MIN_SL_PCT` × `MARGIN_PCT_ALT×LEVERAGE` сошлись в точку, отклоняли каждую сделку с SL на полу).

---

## 9. ENV-справочник

| Переменная | Дефолт | Смысл |
|---|---|---|
| `SA_ATR_DIST` / `SA_ATR_DIST_MAX` | 1.5 / 2.2 | границы растяжения от VWAP |
| `SA_MIN_RR` | 0.7 | минимальный RR входа |
| `SA_RSI_LO` / `SA_RSI_HI` | 30 / 70 | пороги RSI |
| `SA_TIMEOUT_MFE` | 0.40 | порог MFE для smart-timeout |
| `SA_RISK_MULT` | 0.5 | множитель риска SA (BingX), v54 |
| `MARGIN_PCT_SA` / `MARGIN_PCT_ALT` | 0.30 / 0.15 | лимит маржи на сделку (код-дефолт; в проде исторически видели override 0.20 для ALT — источник §2.9.4) |
| `RB_RANGE_BARS` | 48 | окно диапазона RB |
| `RB_MAX_RANGE_ATR` | **8.0** (было 5.0, v56) | гейт флэта RB — временный, ждёт эмпирической калибровки по `[RB SCAN]` логам |
| `RB_SWEEP_MIN_ATR` / `MAX` | 0.15 / 1.0 | глубина свипа |
| `RB_VOL_MIN` / `RB_MIN_RR` | 1.5 / 1.0 | объём и RR для RB |
| `MAX_TRADE_MIN_SA` | 150 | таймаут SA |
| `AI_BLOCK` | **1** (v58) | `0` → AI-оракул всегда advisory, никогда не блокирует вход |
| `RISK_PER_TRADE` / `MAX_TOTAL_POS` / `MAX_PER_DIR` / `DAILY_DD_LIMIT` | 0.01 / 3 / 2 / 0.025 | риск-параметры (BingX) |
| `RISK_PCT` (bybit_worker) | 0.005 | риск на сделку SA на Bybit (было 0.02, v54) |

---

## 10. История версий

| Версия | Изменение |
|---|---|
| v37–v41 | фильтр объёма SA; smart-timeout; логирование `htf_trend`/`mfe_time_min`; qty ROUND; воркер SA-only; сегмент BTC-тренд |
| v42 | потолок `vwap_dist ≤ 2.2`; smart-timeout `MFE ≤ 0.40` |
| v43 | фикс BE при неделимом лоте |
| v44 | гейт `SA_MIN_RR = 0.7`; лог `entry_rr`; промпт оракула |
| v45 | лимиты маржи в ENV |
| v46 | `[SA SCAN]` диагностика; `SA_MIN_RR 0.7→0.5` ← ошибка, откат в v53 |
| v47 | фикс регрессии daily-reset воркера; shadow-стратегия RB ← с фатальным багом |
| v48–v52 | починка `/stats_analyze`; HTML-экранирование TG |
| v53 | фикс среза RB (§2.1); откат `SA_MIN_RR → 0.7` (§2.2); shadow-логирование отсеянных SA-сетапов (`SA_SHADOW`) |
| v54 | риск SA снижен вдвое (`SA_RISK_MULT=0.5`, воркер `RISK_PCT=0.005`) — edge под вопросом, депозит горит вдвое медленнее |
| v55 | RB вынесен в круглосуточный сканер `scan_rb()`; median вместо mean для SA `vol_ratio`; составные флаги нарушений `[SA SCAN]` |
| v56 | `tp50_hit`/`be_moved` только после подтверждённого BE на бирже (§2.7, оба бота); `RB_MAX_RANGE_ATR` 5.0→8.0 + логирование распределения |
| v57 | median вместо mean для SMC/RSI/MOM/PB/RB объёма (§2.6); `SA_SHADOW` расширен на `vwap_far`/`no_setup` + `atr_pct`/`dist_pct` |
| v58 | логирование всех немых `return` в `execute()` (§2.9.2); защита от шаблонных галлюцинаций оракула + `AI_BLOCK` (§2.9.3) |
| v59 | notional-лимит урезает позицию вместо отказа (§2.9.4); тот же фикс (TRUNCATE вместо ROUND) в существующем capping bybit_worker.py; новое правило методологии — проверка пересечения лимитов (§2.9.5, §8.8) |

---

## 11. Критерии готовности к проп-отбору

- [ ] 20–30 сделок SA после стабилизации фильтров с WR ≥ 60% и PF ≥ 1.8
- [ ] Equity curve растёт
- [ ] Max consecutive losses ≤ 4
- [ ] Ноль инцидентов рассинхрона BingX/Bybit
- [ ] Форвард-подтверждение edge на данных, не участвовавших в подборе фильтров
- [ ] SA_SHADOW n≥30 по зонам `vol<1.3`/`RR<0.7`/`dist 1.5-2.2`/`ATR<0.2%` (сейчас n=15, направление подтверждено — не расширять фильтры)

**Текущая оценка: не готов, но диагностический и защитный слой значительно окреп (v53–v59).** Открытый вопрос — SA/SMC edge на форварде (§2.3/§2.4) — требует новых данных, не кодовых фиксов; фильтры не трогать до n≥30.
