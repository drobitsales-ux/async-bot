# Торговый бот: описание работы, стратегии, фильтры
> Код: **async_bot v52 / bybit_worker v47+** | Документ от 2026-07-31 (аудит всех стратегий)
> Раздел §2 — результаты аудита, читать первым.

---

## 1. Архитектура

| Компонент | Роль | Биржа |
|---|---|---|
| `async_bot.py` | Генератор сигналов, все стратегии, БД сделок, отчёты | BingX |
| `bybit_worker.py` | Исполнитель сигналов, **только SA** | Bybit (боевой проп-счёт) |

**Поток:** `async_bot` сканирует рынок → фильтры → AI-оракул (advisory) → открытие на BingX → HTTP-публикация воркеру → воркер применяет свой risk-сайзинг и открывает на Bybit.

**Правило синхронизации:** логика управления позицией из `async_bot.py` обязана зеркалиться в `bybit_worker.py`. Исторически главный источник багов.

**Изоляция воркера (v40):** `ALLOWED_STRATEGIES = ('SA',)`.

---

## 2. 🔴 РЕЗУЛЬТАТЫ АУДИТА 2026-07-31 (главный раздел)

### 2.1. Фатальный баг: стратегия RB не может дать сигнал НИКОГДА

В `rb_signal()` окно диапазона считается срезом, который **включает саму свип-свечу**:

```python
range_low = np.min(l[-(RB_RANGE_BARS + 1):-1])   # = l[-49:-1] → включает бар [-2]
lo2 = l[-2]
if lo2 < range_low:                              # ← НИКОГДА не истинно
```

Срез `[-49:-1]` исключает только текущий формирующийся бар `[-1]`, но оставляет `[-2]`. Значит `range_low ≤ lo2` по определению минимума, и условие пробоя не выполнится ни при каких данных. Зеркально для `hi2 > range_high`.

**Подтверждение в логах:** `[RB SCAN] no_range:62-70 no_sweep:10-18 vol:0 low_rr:0 → SHADOW:0` — счётчики `vol` и `low_rr` **всегда ноль**, до них не доходит ни один кандидат. За 3 суток ноль shadow-сделок.

**Исправление:** срез должен исключать оба бара — `[-(RB_RANGE_BARS + 2):-2]`.

### 2.2. Ошибка калибровки: SA_MIN_RR 0.7→0.5 (v46) навредил

Сегмент «RR входа» дал прямой вердикт: **`lt0.7: 7 сд | WR 0% | Avg −2.54% | PF 0.00`**.

Все 7 сделок с RR < 0.7 существуют только потому, что порог был снижен в v46. Ни одной победы. P(0 побед из 7 | даже WR 40%) = 0.028.

**Вывод:** порог возвращается на 0.7. Обоснование снижения («зона 1.5–2.2 ATR имеет WR 74–80%, значит безубыточный RR ниже») опиралось на in-sample оценку WR, которая не подтвердилась форвардом (см. 2.3).

### 2.3. ⚠️ Главное: «золотая зона» 1.8–2.2 ATR распалась на форварде

| | было (n=20) | стало (n=28) | новые 8 сделок |
|---|---|---|---|
| WR | 80% | **61%** | **1 победа из 8** |
| PF | 14.41 | **1.00** | — |

P(≤1 победы из 8 | WR 80%) = **0.00008**. Гипотеза «зона даёт WR 80%» форвардом опровергнута.

Это **тот же сценарий, что с PB**: сегмент, найденный на исторических данных, показал блестящие цифры на выборке, где он был обнаружен, и схлопнулся к PF ≈ 1.0 при поступлении новых данных. Фильтр v42 (потолок 2.2 ATR) построен на этом же in-sample наблюдении.

### 2.4. Общая деградация всех живых стратегий

| Метрика | было | стало |
|---|---|---|
| SA PF | 1.85 | **1.16** |
| SA WR | 51% | **46%** |
| SA Long PF | 1.89 | **0.86** (убыточен) |
| SMC PF | 1.70 | **1.01** |
| SMC WR | 25% | **21%** |

Новые 8 сделок SA: 1 победа, 7 поражений. P(≤1 из 8 | WR 51%) = 0.031. Обе стратегии сходятся к PF ≈ 1.0 — статистическому отсутствию преимущества.

### 2.5. Нижняя граница объёма SA (1.3) никогда не была валидирована

Фильтр: `if not (1.3 <= vol_ratio <= 2.0)`. Статистическое доказательство (Fisher p=0.0002) сравнивало **1.3–2.0 против 2.0+** — про зону **< 1.3 данных нет вообще**, потому что бот туда не заходил и бакета в отчёте не существует.

Сейчас именно нижняя граница блокирует SA: в логах `vol: 0.68, 0.76, 0.83, 0.86, 1.19` — все ниже 1.3, при этом `vol_climax:1` каждый цикл. Порог 1.3 пришёл из v29 без статистического обоснования.

**Нельзя просто снизить** — это повторило бы ошибку §2.2. Правильный путь: shadow-логирование отсеянных сетапов для сбора данных о зоне < 1.3 без риска деньгами.

### 2.6. RSI: ноль сделок за всю историю

В БД: SMC = 28 сделок, RSI = **0**. Стратегия не сработала ни разу.

Цепочка: объём отсекает 73–77 из 80 (`vol_ratio = mean(v[-4:-1]) / mean(v[-23:-5]) ≥ 1.15–1.25`), оставшиеся 3–7 отсекаются на `rsi_mid` (RSI между 30 и 70). Счётчики `hook/mom/sma/vwap/trend/sl/sqz/pat` — стабильно нули.

**Конструктивное противоречие:** фильтр требует **устойчиво повышенного объёма 3 бара подряд** — сигнатура *тренда*. Стратегия ловит *истощение* (RSI ≤ 30 / ≥ 70), наступающее **после** объёмного климакса, когда объём уже нормализовался. Требования конфликтуют по механике рынка, а не по значению порога.

### 2.7. Аномалия длительности SL-сделок

`SL: 20 сд | Avg −1.93% | 379 мин` при `MAX_TRADE_MIN_SA = 150`.

Причина в коде: `if dur_min > max_dur and not pos.get('tp50_hit')` — сделки со сработавшим TP50 **освобождаются от таймаута** и живут до SL/TP/трейла. Но после TP50 стоп обязан стоять в БУ, и убыток должен быть ≈ 0, а не −1.93%. Требует диагностики: либо BE не применяется, либо трейл отодвигает стоп назад.

### 2.8. Мёртвый код

`_min_quote = 5000.0 if not False else 20000.0` (строка ~1340) — условие константное, всегда 5000.

---

## 3. Стратегия SA — Single-Asset VWAP Mean Reversion

**Инструмент:** BTC/USDT:USDT. **ТФ:** 15m. **Статус:** live, **edge под вопросом** (§2.3).

### Фильтры входа (порядок в коде важен — срабатывает первый)

| № | Фильтр | Значение | Reason | Статус обоснования |
|---|---|---|---|---|
| 1 | Объём | `1.3 ≤ vol_ratio ≤ 2.0` | `vol_climax` | Верхняя граница доказана (Fisher p=0.0002, n=63). **Нижняя (1.3) не валидирована** — §2.5 |
| 2 | Потолок дистанции | `\|dist_atr\| ≤ 2.2` | `vwap_far` | Был p=0.007 in-sample, **форвардом не подтверждён** — §2.3 |
| 3 | Сетап | `\|dist_atr\| ≥ 1.5` и RSI ≤ 30 / ≥ 70 | `no_setup` | базовое определение |
| 4 | Минимальный RR | `RR ≥ 0.7` (после отката v46) | `low_rr` | **Подтверждён форвардом**: RR<0.7 → 0/7 побед |

### Расчёт уровней

```
sl_pct = clip(ATR/price × 2.0, MIN_SL_PCT=1.0%, MAX_SL_PCT=2.5%)
TP     = VWAP (плавающая цель)
RR     = |TP − price| / |price − SL|
```

**Структурное следствие:** SL зажат снизу в 1.0%, TP сжимается вместе с ATR. При ATR < 0.32% цены RR не достигает 0.7 на потолке 2.2 ATR → окно входа пустое. Настоящий корень — немасштабируемый `MIN_SL_PCT`.

### Логика выхода

| Механизм | Условие |
|---|---|
| TP50 | ≥50% пути к TP → фиксация 50% + SL→БУ |
| TP50 при неделимом лоте (v43) | `qty=0.001` → весь объём, SL→БУ |
| Фронтран БУ | 80–100% пути к VWAP → SL→БУ |
| Smart Timeout | >75 мин, не tp50_hit, pnl≤0, MFE≤0.40% |
| Обычный таймаут | >150 мин **и не tp50_hit** (см. §2.7) |
| Трейлинг | после TP100, 0.8×ATR |

---

## 4. SMC — Smart Money Concepts (BingX)

28 сделок, WR 21%, **PF 1.01** (был 1.70). Long PF 1.10, Short PF 0.66.

Цепочка: `news → vol (режет 63–78 из 80) → struct → choch → short_blk → vwap/rsi/adx → rsi_exh → alt_hi → fvg`. Цепочка рвётся на объёме и структуре; `fvg` стабильно 0.

**Открытый вопрос (с v44):** сделка ADA вошла при RSI 66.1 при заявленной зоне 55–65. Бакет `65+`: 5 сд, PF 0.01. Диагностика не завершена.

---

## 5. RSI (BingX)

**0 сделок за всю историю.** Разбор — §2.6.

---

## 6. Shadow / отключённые

| Стратегия | Статус | Причина |
|---|---|---|
| **RB** | shadow, **не работает** | Фатальный баг среза (§2.1) |
| **PB** | shadow, в live не выводить | 200 сд: Long PF 0.52, Short PF 0.41. Все «кандидаты» схлопнулись при росте n (ADX 60+: WR 71%→48%, PF 2.76→1.36) |
| **MOM** | отключена | 222 shadow-сделки, edge нет |
| **1h-бот** | отключён | 154 сделки, edge нет |

---

## 7. Риск-менеджмент

| Механизм | Значение |
|---|---|
| Риск на сделку | 1% депозита (`qty = risk/sl_dist`) |
| Плечо | 5x |
| Максимум позиций | 2 |
| Дневной лимит DD | 2.5% |
| Лимит маржи | `MARGIN_PCT_SA=0.30`, `MARGIN_PCT_ALT=0.20` (ENV) |
| Округление лота | `ccxt.ROUND` (дефолт ccxt — TRUNCATE, терял до 44%) |
| Проп-guard | пропуск при факт. риске > 1.30× целевого |

### Ограничения вне кода

**Неделимый лот BTC.** При депозите ~$78 и риске 1% позиция Bybit = 0.001 (минимум). TP50 невозможен, обход — весь объём под БУ (v43). Заработает при депозите ≈ $130–150. Форсировать риском нельзя: 1.68% депозита = 67% дневного лимита DD за сделку.

**AI-оракул ненадёжен.** Задокументированы галлюцинации (`"100/100 | RSI > 68"` при RSI 66.1). Символы `<`/`>` ломали `parse_mode=HTML` и теряли уведомления (v49). Используется как advisory.

---

## 8. Методология (нарушения обошлись дорого)

1. **n ≥ 30 до внедрения**, n ≥ 15 для кандидата к наблюдению.
2. **Fisher exact + Wilson CI.** Пересекающиеся CI = фильтра нет.
3. **In-sample ≠ forward.** Сегмент, найденный на выборке, обязан подтвердиться на новых данных до внедрения фильтра. Нарушено дважды: PB (ADX 60+), SA (зона 1.8–2.2) — оба раза edge исчез.
4. **Проверять популяцию калибровки.** Порог, откалиброванный на одной популяции, нельзя применять к другой (ошибка SA_MIN_RR, §2.2).
5. **Механическое обоснование обязательно.**
6. **Одно изменение — один деплой.**
7. **Отсутствие данных ≠ отсутствие edge.** Зона vol < 1.3 не изучена, потому что фильтр туда не пускает. Собирать shadow, а не гадать.

---

## 9. ENV-справочник

| Переменная | Дефолт | Смысл |
|---|---|---|
| `SA_ATR_DIST` / `SA_ATR_DIST_MAX` | 1.5 / 2.2 | границы растяжения от VWAP |
| `SA_MIN_RR` | **0.7** (откат v46) | минимальный RR входа |
| `SA_RSI_LO` / `SA_RSI_HI` | 30 / 70 | пороги RSI |
| `SA_TIMEOUT_MFE` | 0.40 | порог MFE для smart-timeout |
| `MARGIN_PCT_SA` / `MARGIN_PCT_ALT` | 0.30 / 0.20 | лимит маржи на сделку |
| `RB_RANGE_BARS` | 48 | окно диапазона RB |
| `RB_MAX_RANGE_ATR` | 5.0 | гейт флэта RB |
| `RB_SWEEP_MIN_ATR` / `MAX` | 0.15 / 1.0 | глубина свипа |
| `RB_VOL_MIN` / `RB_MIN_RR` | 1.5 / 1.0 | объём и RR для RB |
| `MAX_TRADE_MIN_SA` | 150 | таймаут SA |
| `RISK_PCT` / `MAX_POS` / `DAILY_DD` | 0.01 / 2 / 0.025 | риск-параметры |

---

## 10. История версий

| Версия | Изменение |
|---|---|
| v37–v41 | фильтр объёма SA; smart-timeout; логирование `htf_trend`/`mfe_time_min`; qty ROUND; воркер SA-only; сегмент BTC-тренд |
| v42 | потолок `vwap_dist ≤ 2.2`; smart-timeout `MFE ≤ 0.40` |
| v43 | фикс BE при неделимом лоте |
| v44 | гейт `SA_MIN_RR = 0.7`; лог `entry_rr`; промпт оракула |
| v45 | лимиты маржи в ENV |
| v46 | `[SA SCAN]` диагностика; `SA_MIN_RR 0.7→0.5` ← **ошибка, откат в v53** |
| v47 | фикс регрессии daily-reset воркера; shadow-стратегия RB ← **с фатальным багом** |
| v48–v52 | починка `/stats_analyze`; HTML-экранирование TG |
| **v53 (план)** | фикс среза RB; откат `SA_MIN_RR → 0.7`; shadow-логирование отсеянных сетапов |

---

## 11. Критерии готовности к проп-отбору

- [ ] 20–30 сделок SA после стабилизации фильтров с WR ≥ 60% и PF ≥ 1.8
- [ ] Equity curve растёт (сейчас депозит ниже стартового)
- [ ] Max consecutive losses ≤ 4
- [ ] Ноль инцидентов рассинхрона BingX/Bybit
- [ ] Форвард-подтверждение edge на данных, не участвовавших в подборе фильтров

**Текущая оценка: не готов.** PF всех живых стратегий сошёлся к ≈1.0; ключевой сегмент, на котором строилась гипотеза преимущества SA, форвардом не подтверждён.
